<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="RobustFiler.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:RobustFiler"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="RobustFiler"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="32" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <!-- Windows 11 system caption buttons width is approx 138px -->
                <ColumnDefinition Width="140" /> 
            </Grid.ColumnDefinitions>

            <!-- Window Icon & Title -->
            <StackPanel Orientation="Horizontal" Grid.Column="0" Spacing="12" Margin="12,0,0,0" IsHitTestVisible="False">
                <Image Source="Assets/AppIcon.ico" Width="16" Height="16" VerticalAlignment="Center" />
                <TextBlock Text="RobustFiler" VerticalAlignment="Center" FontSize="12" />
            </StackPanel>

            <!-- Dual Pane Button -->
            <!-- Placed before the caption buttons -->
            <Button Grid.Column="2"
                    Command="{x:Bind ViewModel.ToggleDualPaneCommand}"
                    Background="Transparent"
                    BorderThickness="0"
                    Width="36" Height="32"
                    ToolTipService.ToolTip="デュアルペイン">
                <!-- Make icon smaller -->
                <Viewbox Width="14" Height="14">
                    <SymbolIcon Symbol="TwoPage" />
                </Viewbox>
            </Button>
        </Grid>

        <!--
            The Frame hosts pages for your application content. Add your UI to
            MainPage.xaml rather than here so you can use Page features such as
            navigation events and the Loaded lifecycle.
        -->
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
