<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="RobustFiler.Controls.FilePaneControl"
    x:Name="PaneControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:RobustFiler.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:primitives="using:CommunityToolkit.WinUI.UI.Controls.Primitives"
    xmlns:sizers="using:CommunityToolkit.WinUI.Controls"
    xmlns:converters="using:RobustFiler.Converters"
    mc:Ignorable="d"
    FontSize="12">

    <UserControl.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter" />
        <x:Double x:Key="TreeViewItemMinHeight">24</x:Double>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Address Bar Area -->
        <Grid Grid.Row="0" Padding="8" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" FontFamily="Segoe Fluent Icons" Content="&#xE72B;" Command="{x:Bind ViewModel.GoBackCommand}" ToolTipService.ToolTip="戻る" Margin="0,0,4,0"/>
            <Button Grid.Column="1" FontFamily="Segoe Fluent Icons" Content="&#xE72A;" Command="{x:Bind ViewModel.GoForwardCommand}" ToolTipService.ToolTip="進む" Margin="0,0,8,0"/>
            
            <Border Grid.Column="2" x:Name="AddressBarBorder" Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Tapped="AddressBarBorder_Tapped" VerticalAlignment="Stretch" MinHeight="32">
                <Grid>
                    <BreadcrumbBar x:Name="Breadcrumb" ItemsSource="{x:Bind ViewModel.BreadcrumbItems, Mode=OneWay}" ItemClicked="BreadcrumbBar_ItemClicked" VerticalAlignment="Center" Margin="4,0,4,0" Visibility="Visible">
                        <BreadcrumbBar.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:BreadcrumbItem">
                                <TextBlock Text="{x:Bind Name}" />
                            </DataTemplate>
                        </BreadcrumbBar.ItemTemplate>
                    </BreadcrumbBar>
                    <TextBox x:Name="AddressTextBox" Text="{x:Bind ViewModel.CurrentPath, Mode=OneWay}" Visibility="Collapsed" KeyDown="AddressTextBox_KeyDown" LostFocus="AddressTextBox_LostFocus" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" BorderThickness="0" Background="Transparent" />
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="150" MaxWidth="400"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TreeView x:Name="FolderTree" Grid.Column="0" ItemsSource="{x:Bind ViewModel.RootDrives, Mode=OneWay}"
                      ItemInvoked="TreeView_ItemInvoked"
                      DoubleTapped="FolderTree_DoubleTapped"
                      CanDragItems="True"
                      AllowDrop="True"
                      DragItemsStarting="TreeView_DragItemsStarting"
                      DragItemsCompleted="FolderTree_DragItemsCompleted"
                      DragOver="TreeView_DragOver"
                      Drop="TreeView_Drop">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="MinHeight" Value="24" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="ContextFlyout">
                            <Setter.Value>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="お気に入りに追加" Click="MenuFlyoutItem_AddFavorite_Click" />
                                </MenuFlyout>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:FileNodeViewModel">
                        <TreeViewItem ItemsSource="{x:Bind Children, Mode=OneWay}"
                                      IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}">
                            <StackPanel Orientation="Horizontal" Spacing="4" Padding="4,0" Background="Transparent" CornerRadius="4"
                                        PointerEntered="TreeItem_PointerEntered" PointerExited="TreeItem_PointerExited">
                                <Image Source="{x:Bind FileIcon, Mode=OneWay}" Width="16" Height="16" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" />
                            </StackPanel>
                        </TreeViewItem>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <sizers:GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Transparent" />

            <Grid Grid.Column="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="貼り付け" Command="{x:Bind ViewModel.PasteFilesCommand}" />
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem Text="新しいフォルダー" Command="{x:Bind ViewModel.CreateFolderCommand}" />
                    </MenuFlyout>
                </Grid.ContextFlyout>

                <controls:DataGrid x:Name="FileListGrid"
                                   ItemsSource="{x:Bind ViewModel.CurrentFolderItems, Mode=OneWay}" 
                                   AutoGenerateColumns="False" 
                                   Sorting="DataGrid_Sorting"
                                   CanUserSortColumns="True"
                                   IsReadOnly="True"
                                   SelectionMode="Extended"
                                   DoubleTapped="DataGrid_DoubleTapped"
                                   SelectionChanged="DataGrid_SelectionChanged"
                                   LoadingRow="DataGrid_LoadingRow"
                                   UnloadingRow="DataGrid_UnloadingRow"
                                   AllowDrop="True"
                                   DragOver="DataGrid_DragOver"
                                   Drop="DataGrid_Drop"
                                   GridLinesVisibility="None"
                                   RowHeight="24">
                    <controls:DataGrid.ColumnHeaderStyle>
                        <Style TargetType="primitives:DataGridColumnHeader">
                            <Setter Property="ContextFlyout">
                                <Setter.Value>
                                    <MenuFlyout Opening="ColumnMenu_Opening">
                                        <ToggleMenuFlyoutItem Text="名前" Tag="Name" Click="ColumnMenuItem_Click" />
                                        <ToggleMenuFlyoutItem Text="更新日時" Tag="DateModified" Click="ColumnMenuItem_Click" />
                                        <ToggleMenuFlyoutItem Text="サイズ" Tag="Size" Click="ColumnMenuItem_Click" />
                                    </MenuFlyout>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </controls:DataGrid.ColumnHeaderStyle>
                    <controls:DataGrid.RowStyle>
                        <Style TargetType="controls:DataGridRow">
                            <Setter Property="ContextFlyout">
                                <Setter.Value>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="コピー" Click="MenuFlyoutItem_Copy_Click" />
                                        <MenuFlyoutItem Text="切り取り" Click="MenuFlyoutItem_Cut_Click" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="お気に入りに追加" Click="MenuFlyoutItem_AddFavorite_Click" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="名前の変更" Click="MenuFlyoutItem_Rename_Click" />
                                        <MenuFlyoutItem Text="削除" Click="MenuFlyoutItem_Delete_Click" />
                                    </MenuFlyout>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </controls:DataGrid.RowStyle>

                    <controls:DataGrid.Columns>
                        <controls:DataGridTemplateColumn Header="名前" Tag="Name">
                            <controls:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate x:DataType="viewmodels:FileNodeViewModel">
                                    <StackPanel Orientation="Horizontal" Spacing="6" Padding="2,0" Background="Transparent">
                                        <Image Source="{x:Bind FileIcon, Mode=OneWay}" Width="16" Height="16" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellTemplate>
                        </controls:DataGridTemplateColumn>
                        <controls:DataGridTextColumn Header="更新日時" Tag="DateModified" Binding="{Binding DateModified}" />
                        <controls:DataGridTextColumn Header="サイズ" Tag="Size" Binding="{Binding RawSize, Converter={StaticResource FileSizeConverter}}" />
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
